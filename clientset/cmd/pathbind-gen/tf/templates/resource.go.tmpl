// Code generated by pathbind-gen --mode=tf. DO NOT EDIT.
// Source: pathbind-overrides.yaml

package {{.Package}}

{{- $hasImmutable := false}}
{{- $hasComputedID := false}}
{{- $hasComputed := false}}
{{- $hasList := false}}
{{- $hasAnyList := false}}
{{- $hasMap := false}}
{{- $hasAnyMap := false}}
{{- $hasStringModifiers := false}}
{{- $hasBoolModifiers := false}}
{{- $hasInt64Modifiers := false}}
{{- $hasInt32 := false}}
{{- $hasAttributes := false}}
{{- range .AllFields}}
{{- $inSchema := or (ne .Path "-") (and (eq .Path "-") (gt (len .Operations) 0))}}
{{- if and .Immutable $inSchema}}
{{- $hasImmutable = true}}
{{- end}}
{{- if and .Computed (eq .GoName "Id") $inSchema}}
{{- $hasComputedID = true}}
{{- end}}
{{- if and .Computed $inSchema}}
{{- $hasComputed = true}}
{{- end}}
{{- if and (hasSuffix .Type "[]") $inSchema}}
{{- $hasAnyList = true}}
{{- if or .Immutable .Computed}}
{{- $hasList = true}}
{{- end}}
{{- end}}
{{- if and (eq .Type "map") $inSchema}}
{{- $hasAnyMap = true}}
{{- if or .Immutable .Computed}}
{{- $hasMap = true}}
{{- end}}
{{- end}}
{{- if and $inSchema (or .Immutable .Computed)}}
{{- if or (eq .Type "string") (eq .Type "*string")}}
{{- $hasStringModifiers = true}}
{{- else if or (eq .Type "bool") (eq .Type "*bool")}}
{{- $hasBoolModifiers = true}}
{{- else if or (eq .Type "int32") (eq .Type "*int32") (eq .Type "int64") (eq .Type "*int64")}}
{{- $hasInt64Modifiers = true}}
{{- end}}
{{- end}}
{{- if and $inSchema (or (eq .Type "int32") (eq .Type "*int32"))}}
{{- $hasInt32 = true}}
{{- end}}
{{- if $inSchema}}
{{- $hasAttributes = true}}
{{- end}}
{{- end}}
import (
	"context"
	"fmt"
{{- if .Namespaced}}
	"strings"
{{- end}}

{{- if $hasInt32}}
	"math"
{{- end}}

	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/resource/schema"
{{- if or $hasImmutable $hasComputed}}
	"github.com/hashicorp/terraform-plugin-framework/resource/schema/planmodifier"
{{- end}}
{{- if $hasStringModifiers}}
	"github.com/hashicorp/terraform-plugin-framework/resource/schema/stringplanmodifier"
{{- end}}
{{- if $hasBoolModifiers}}
	"github.com/hashicorp/terraform-plugin-framework/resource/schema/boolplanmodifier"
{{- end}}
{{- if $hasInt64Modifiers}}
	"github.com/hashicorp/terraform-plugin-framework/resource/schema/int64planmodifier"
{{- end}}
{{- if $hasList}}
	"github.com/hashicorp/terraform-plugin-framework/resource/schema/listplanmodifier"
{{- end}}
{{- if $hasMap}}
	"github.com/hashicorp/terraform-plugin-framework/resource/schema/mapplanmodifier"
{{- end}}
{{- if or $hasAnyList $hasAnyMap}}
	"github.com/hashicorp/terraform-plugin-framework/types"
{{- end}}
	v1alpha1 "github.com/openshift-online/rosa-hyperfleet-api/api/v1alpha1/public"
	hyperfleet "github.com/openshift-online/rosa-hyperfleet-api/clientset"
	hfwrappers "github.com/openshift-online/rosa-hyperfleet-api/clientset/platform"
	pathbind "github.com/openshift-online/rosa-hyperfleet-api/clientset/pathbind"

	"github.com/terraform-redhat/terraform-provider-rhcs/provider/providerdata"
)

var _ resource.Resource = &{{.ResourceName}}Resource{}
var _ resource.ResourceWithConfigure = &{{.ResourceName}}Resource{}
var _ resource.ResourceWithImportState = &{{.ResourceName}}Resource{}

// {{.ResourceName}}HandlerFactory creates the hand-written handler used by the generated resource.
type {{.ResourceName}}HandlerFactory func(client hyperfleet.Interface, accountID, callerARN string) {{.ResourceName}}Handler

// New{{.ResourceName}} creates a new {{.ResourceName}} resource.
func New{{.ResourceName}}(handlerFactory {{.ResourceName}}HandlerFactory) resource.Resource {
	return &{{.ResourceName}}Resource{handlerFactory: handlerFactory}
}

// {{.ResourceName}}Handler defines consumer-specific logic for {{.ResourceName}} operations.
type {{.ResourceName}}Handler interface {
	// PreExpand is called before pathbind.Expand to validate inputs and derive computed fields.
	// Example: validate name length, compute RolesRef from prefix, derive region from AZs.
	PreExpand(ctx context.Context, input *{{.ResourceName}}State) diag.Diagnostics

	// PostExpand is called after pathbind.Expand and before the API call.
	// Use this to set SDK fields that pathbind cannot express (enum constants, computed structs).
	PostExpand(ctx context.Context, input *{{.ResourceName}}State, obj *v1alpha1.{{.SDKShortType}}) diag.Diagnostics

	// PostResponse is called after API calls (Create, Read, Update, Delete).
	// Use this for output formatting, side effects, and post-processing.
	PostResponse(ctx context.Context, resp *v1alpha1.{{.SDKShortType}}) diag.Diagnostics

	// PostFlatten is called after pathbind.Flatten converts API response to state.
	// Use this to populate computed fields, adjust state values, or perform post-processing
	// that requires access to both the API response and the state being built.
	PostFlatten(ctx context.Context, state *{{.ResourceName}}State, resp *v1alpha1.{{.SDKShortType}})
{{- if .Namespaced}}

	// Namespace derives the K8s namespace for this resource from state fields
	// (e.g. a parent ID). Used by Read/Delete/ImportState, which only have
	// access to Terraform state (no SDK object) before calling the API.
	// Must be a pure, side-effect-free computation (no API calls).
	Namespace(ctx context.Context, state *{{.ResourceName}}State) string
{{- end}}
}

// {{.ResourceName}}Resource manages {{.ResourceName}} resources via the hyperfleet Platform API.
type {{.ResourceName}}Resource struct {
	// Client is the hyperfleet SDK client configured by the provider.
	Client hyperfleet.Interface

	// AccountID is the AWS account ID configured by the provider.
	AccountID string

	// CallerARN is the SigV4 caller ARN configured by the provider.
	CallerARN string

	// Handler is the consumer-provided implementation of {{.ResourceName}}Handler.
	Handler {{.ResourceName}}Handler

	handlerFactory {{.ResourceName}}HandlerFactory
}

func (r *{{.ResourceName}}Resource) Metadata(
	_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse,
) {
	resp.TypeName = req.ProviderTypeName + "_{{lower .ResourceName}}_hyperfleet"
}

func (r *{{.ResourceName}}Resource) Configure(
	_ context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse,
) {
	if req.ProviderData == nil {
		return
	}

	shared, ok := req.ProviderData.(*providerdata.ProviderSharedData)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Resource Configure Type",
			fmt.Sprintf("Expected *providerdata.ProviderSharedData, got: %T. Please report this issue to the provider developers.", req.ProviderData),
		)
		return
	}

	r.Client = shared.HyperfleetClient
	r.AccountID = shared.HyperfleetAccountID
	r.CallerARN = shared.HyperfleetCallerARN

	// Initialize the handler via factory function
	r.Handler = r.handlerFactory(shared.HyperfleetClient, shared.HyperfleetAccountID, shared.HyperfleetCallerARN)
}

func (r *{{.ResourceName}}Resource) Schema(
	_ context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse,
) {
	resp.Schema = schema.Schema{
		MarkdownDescription: "Manages {{.ResourceName}} resources.",
		Attributes: map[string]schema.Attribute{
{{- range .AllFields}}
{{- if isConsumerOnly .}}
			"{{attrName .}}": schema.StringAttribute{
				MarkdownDescription: "Resource identifier (computed).",
				Computed:            true,
			},
{{- else}}
			"{{attrName .}}": schema.{{schemaType .}}Attribute{
				MarkdownDescription: "{{.GoName}}.",
{{- if .Computed}}
				Computed:    true,
{{- else if .Required}}
				Required:    true,
{{- else}}
				Optional:    true,
{{- end}}
{{- if .Sensitive}}
				Sensitive:   true,
{{- end}}
{{- if eq (schemaType .) "List"}}
				ElementType: types.StringType,
{{- if or .Immutable .Computed}}
				PlanModifiers: []planmodifier.List{
{{- if .Immutable}}
					listplanmodifier.RequiresReplace(),
{{- end}}
{{- if .Computed}}
					listplanmodifier.UseStateForUnknown(),
{{- end}}
				},
{{- end}}
{{- else if eq (schemaType .) "Map"}}
				ElementType: types.StringType,
{{- if or .Immutable .Computed}}
				PlanModifiers: []planmodifier.Map{
{{- if .Immutable}}
					mapplanmodifier.RequiresReplace(),
{{- end}}
{{- if .Computed}}
					mapplanmodifier.UseStateForUnknown(),
{{- end}}
				},
{{- end}}
{{- else if eq (schemaType .) "Bool"}}
{{- if or .Immutable .Computed}}
				PlanModifiers: []planmodifier.Bool{
{{- if .Immutable}}
					boolplanmodifier.RequiresReplace(),
{{- end}}
{{- if .Computed}}
					boolplanmodifier.UseStateForUnknown(),
{{- end}}
				},
{{- end}}
{{- else if eq (schemaType .) "Int64"}}
{{- if or .Immutable .Computed}}
				PlanModifiers: []planmodifier.Int64{
{{- if .Immutable}}
					int64planmodifier.RequiresReplace(),
{{- end}}
{{- if .Computed}}
					int64planmodifier.UseStateForUnknown(),
{{- end}}
				},
{{- end}}
{{- else if eq (schemaType .) "String"}}
{{- if and (planModifiers .) (or .Immutable .Computed)}}
				PlanModifiers: []planmodifier.String{
					{{planModifiers .}},
				},
{{- end}}
{{- end}}
			},
{{- end}}
{{- end}}
		},
	}
}

func (r *{{.ResourceName}}Resource) Create(
	ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse,
) {
	// Validate that hyperfleet is configured
	if r.Client == nil {
		resp.Diagnostics.AddError(
			"Hyperfleet not configured",
			//nolint:lll
			"The hyperfleet_url provider attribute is required to manage {{.ResourceName}} resources. Set it explicitly in the provider configuration or via the RHCS_HYPERFLEET_URL environment variable.",
		)
		return
	}

	var plan {{.ResourceName}}State
	resp.Diagnostics.Append(req.Plan.Get(ctx, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Call handler for validation and derivation
	diags := r.Handler.PreExpand(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Convert Terraform state to native Go struct for pathbind
	native, conversionDiags := terraform{{.ResourceName}}ToNative(&plan)
	resp.Diagnostics.Append(conversionDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Create an empty SDK object and expand the plan into it.
	obj := &v1alpha1.{{.SDKShortType}}{}
	if err := pathbind.Expand(ctx, native, obj); err != nil {
		resp.Diagnostics.AddError("Failed to expand plan to SDK struct", err.Error())
		return
	}

	// Call handler for setting unmappable fields
	diags = r.Handler.PostExpand(ctx, &plan, obj)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Create via API
	// For namespaced resources, obj.GetNamespace() reflects the value the handler's
	// PostExpand set on the SDK object; passing it here keeps the client-scoped
	// namespace and the object's namespace in sync.
	created, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}({{if .Namespaced}}obj.GetNamespace(){{end}}).Create(ctx, obj, hfwrappers.CreateOptions{})
	if err != nil {
		resp.Diagnostics.AddError("Failed to create {{.ResourceName}}", err.Error())
		return
	}

	// Flatten response into native state
	var nativeState {{.ResourceName}}StateNative
	if err := pathbind.Flatten(ctx, created, &nativeState); err != nil {
		resp.Diagnostics.AddError("Failed to flatten API response to state", err.Error())
		return
	}

	// Call handler for post-processing
	diags = r.Handler.PostResponse(ctx, created)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Convert native state to Terraform state
	responseState := native{{.ResourceName}}ToTerraform(&nativeState)
	
	// Merge API response values into plan to preserve consumer-only fields
	// This keeps user-provided values while adding computed fields from the API
	mergeResponse{{.ResourceName}}(&plan, responseState)
	
	// Call handler to populate computed fields and adjust state after flatten
	r.Handler.PostFlatten(ctx, &plan, created)
	
	resp.Diagnostics.Append(resp.State.Set(ctx, &plan)...)
}

func (r *{{.ResourceName}}Resource) Read(
	ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse,
) {
	// Validate that hyperfleet is configured
	if r.Client == nil {
		resp.Diagnostics.AddError(
			"Hyperfleet not configured",
			//nolint:lll
			"The hyperfleet_url provider attribute is required to manage {{.ResourceName}} resources. Set it explicitly in the provider configuration or via the RHCS_HYPERFLEET_URL environment variable.",
		)
		return
	}

	var state {{.ResourceName}}State
	resp.Diagnostics.Append(req.State.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Get current resource from API
{{- if .Namespaced}}
  obj, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}(r.Handler.Namespace(ctx, &state)).Get(ctx, state.{{.IdentifierField}}.ValueString(), hfwrappers.GetOptions{})
{{- else}}
  obj, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}().Get(ctx, state.{{.IdentifierField}}.ValueString(), hfwrappers.GetOptions{})
{{- end}}
	if err != nil {
		if isNotFound(err) {
			resp.State.RemoveResource(ctx)
			return
		}
		resp.Diagnostics.AddError("Failed to read {{.ResourceName}}", err.Error())
		return
	}

	// Flatten response into native state
	var nativeState {{.ResourceName}}StateNative
	if err := pathbind.Flatten(ctx, obj, &nativeState); err != nil {
		resp.Diagnostics.AddError("Failed to flatten API response to state", err.Error())
		return
	}

	// Call handler for post-processing
	diags := r.Handler.PostResponse(ctx, obj)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Convert native state to Terraform state
	responseState := native{{.ResourceName}}ToTerraform(&nativeState)

	// Merge API response values into state to preserve consumer-only fields
	// (fields with no SDK path, e.g. immutable creation-only inputs) that
	// would otherwise be lost since they cannot be reconstructed from the API response.
	mergeResponse{{.ResourceName}}(&state, responseState)

	// Call handler to populate computed fields and adjust state after flatten
	r.Handler.PostFlatten(ctx, &state, obj)

	resp.Diagnostics.Append(resp.State.Set(ctx, &state)...)
}

func (r *{{.ResourceName}}Resource) Update(
	ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse,
) {
	// Validate that hyperfleet is configured
	if r.Client == nil {
		resp.Diagnostics.AddError(
			"Hyperfleet not configured",
			//nolint:lll
			"The hyperfleet_url provider attribute is required to manage {{.ResourceName}} resources. Set it explicitly in the provider configuration or via the RHCS_HYPERFLEET_URL environment variable.",
		)
		return
	}

	var plan {{.ResourceName}}State
	resp.Diagnostics.Append(req.Plan.Get(ctx, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Call handler for validation and derivation
	diags := r.Handler.PreExpand(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Convert Terraform state to native Go struct for pathbind
	native, conversionDiags := terraform{{.ResourceName}}ToNative(&plan)
	resp.Diagnostics.Append(conversionDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Fetch the current object so fields not represented in Terraform are preserved.
{{- if .Namespaced}}
  obj, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}(r.Handler.Namespace(ctx, &plan)).Get(ctx, plan.{{.IdentifierField}}.ValueString(), hfwrappers.GetOptions{})
{{- else}}
  obj, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}().Get(ctx, plan.{{.IdentifierField}}.ValueString(), hfwrappers.GetOptions{})
{{- end}}
	if err != nil {
		resp.Diagnostics.AddError("Failed to read {{.ResourceName}} for update", err.Error())
		return
	}

	// Expand the Terraform plan into the current SDK object.
	if err := pathbind.Expand(ctx, native, obj); err != nil {
		resp.Diagnostics.AddError("Failed to expand plan to SDK struct", err.Error())
		return
	}

	// Call handler for setting unmappable fields
	diags = r.Handler.PostExpand(ctx, &plan, obj)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Update via API
	updated, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}({{if .Namespaced}}obj.GetNamespace(){{end}}).Update(ctx, obj, hfwrappers.UpdateOptions{})
	if err != nil {
		resp.Diagnostics.AddError("Failed to update {{.ResourceName}}", err.Error())
		return
	}

	// Flatten response into native state
	var nativeState {{.ResourceName}}StateNative
	if err := pathbind.Flatten(ctx, updated, &nativeState); err != nil {
		resp.Diagnostics.AddError("Failed to flatten API response to state", err.Error())
		return
	}

	// Call handler for post-processing
	diags = r.Handler.PostResponse(ctx, updated)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Convert native state to Terraform state
	responseState := native{{.ResourceName}}ToTerraform(&nativeState)
	
	// Merge API response values into plan to preserve consumer-only fields
	// This keeps user-provided values while adding computed fields from the API
	mergeResponse{{.ResourceName}}(&plan, responseState)
	
	// Call handler to populate computed fields and adjust state after flatten
	r.Handler.PostFlatten(ctx, &plan, updated)
	
	resp.Diagnostics.Append(resp.State.Set(ctx, &plan)...)
}

func (r *{{.ResourceName}}Resource) Delete(
	ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse,
) {
	// Validate that hyperfleet is configured
	if r.Client == nil {
		resp.Diagnostics.AddError(
			"Hyperfleet not configured",
			//nolint:lll
			"The hyperfleet_url provider attribute is required to manage {{.ResourceName}} resources. Set it explicitly in the provider configuration or via the RHCS_HYPERFLEET_URL environment variable.",
		)
		return
	}

	var state {{.ResourceName}}State
	resp.Diagnostics.Append(req.State.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}({{if .Namespaced}}r.Handler.Namespace(ctx, &state){{end}}).Delete(ctx, state.{{.IdentifierField}}.ValueString(), hfwrappers.DeleteOptions{})
	if err != nil {
		resp.Diagnostics.AddError("Failed to delete {{.ResourceName}}", err.Error())
		return
	}

	// Call handler for post-delete side effects
	var emptyResp *v1alpha1.{{.SDKShortType}}
	diags := r.Handler.PostResponse(ctx, emptyResp)
	resp.Diagnostics.Append(diags...)
}

func (r *{{.ResourceName}}Resource) ImportState(
	ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse,
) {
	// Validate that hyperfleet is configured
	if r.Client == nil {
		resp.Diagnostics.AddError(
			"Hyperfleet not configured",
			//nolint:lll
			"The hyperfleet_url provider attribute is required to manage {{.ResourceName}} resources. Set it explicitly in the provider configuration or via the RHCS_HYPERFLEET_URL environment variable.",
		)
		return
	}

	// Use the ID passed in as the {{.ResourceName}} identifier
{{- if .Namespaced}}
	parts := strings.SplitN(req.ID, "/", 2)
	if len(parts) != 2 || parts[0] == "" || parts[1] == "" {
		resp.Diagnostics.AddError("Invalid {{.ResourceName}} import ID", "expected namespace/id")
		return
	}
	namespace := parts[0]
	if !strings.HasPrefix(namespace, "cluster-") {
		namespace = "cluster-" + namespace
	}
	obj, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}(namespace).Get(ctx, parts[1], hfwrappers.GetOptions{})
{{- else}}
	obj, err := r.Client.HyperfleetV1alpha1().{{pluralize .ResourceName}}().Get(ctx, req.ID, hfwrappers.GetOptions{})
{{- end}}
	if err != nil {
		resp.Diagnostics.AddError("Failed to import {{.ResourceName}}", err.Error())
		return
	}

	// Flatten response into native state
	var nativeState {{.ResourceName}}StateNative
	if err := pathbind.Flatten(ctx, obj, &nativeState); err != nil {
		resp.Diagnostics.AddError("Failed to flatten API response to state", err.Error())
		return
	}

	// Call handler for post-processing
	diags := r.Handler.PostResponse(ctx, obj)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Convert native state to Terraform state
	state := native{{.ResourceName}}ToTerraform(&nativeState)

	// Call handler to populate computed fields and adjust state after flatten
	r.Handler.PostFlatten(ctx, state, obj)

	resp.Diagnostics.Append(resp.State.Set(ctx, state)...)
}

// Helper functions for state conversion

// terraform{{.ResourceName}}ToNative converts Terraform framework types to native Go types.
// This is used before pathbind.Expand to prepare input for API calls.
func terraform{{.ResourceName}}ToNative(tf *{{.ResourceName}}State) (*{{.ResourceName}}StateNative, diag.Diagnostics) {
	if tf == nil {
		return nil, nil
	}
	native := &{{.ResourceName}}StateNative{}
	var diags diag.Diagnostics
{{- range .AllFields}}
{{- if hasSuffix .Type "[]"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		items, collectionDiags := terraformListToStringSlice(tf.{{.GoName}})
		diags = append(diags, collectionDiags...)
		native.{{.GoName}} = items
	}
{{- else if eq .Type "string"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		native.{{.GoName}} = tf.{{.GoName}}.ValueString()
	}
{{- else if eq .Type "*string"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		s := tf.{{.GoName}}.ValueString()
		native.{{.GoName}} = &s
	}
{{- else if eq .Type "bool"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		native.{{.GoName}} = tf.{{.GoName}}.ValueBool()
	}
{{- else if eq .Type "*bool"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		b := tf.{{.GoName}}.ValueBool()
		native.{{.GoName}} = &b
	}
{{- else if eq .Type "int32"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		value := tf.{{.GoName}}.ValueInt64()
		if value < math.MinInt32 || value > math.MaxInt32 {
			diags.AddError("Invalid int32 value", fmt.Sprintf("field {{.GoName}} value %d is outside the int32 range", value))
		} else {
			native.{{.GoName}} = int32(value)
		}
	}
{{- else if eq .Type "*int32"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		value := tf.{{.GoName}}.ValueInt64()
		if value < math.MinInt32 || value > math.MaxInt32 {
			diags.AddError("Invalid int32 value", fmt.Sprintf("field {{.GoName}} value %d is outside the int32 range", value))
		} else {
			i := int32(value)
			native.{{.GoName}} = &i
		}
	}
{{- else if eq .Type "int64"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		native.{{.GoName}} = tf.{{.GoName}}.ValueInt64()
	}
{{- else if eq .Type "*int64"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		i := tf.{{.GoName}}.ValueInt64()
		native.{{.GoName}} = &i
	}
{{- else if eq .Type "map"}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		values, collectionDiags := terraformMapToStringMap(tf.{{.GoName}})
		diags = append(diags, collectionDiags...)
		native.{{.GoName}} = values
	}
{{- else}}
	if !tf.{{.GoName}}.IsNull() && !tf.{{.GoName}}.IsUnknown() {
		native.{{.GoName}} = tf.{{.GoName}}.ValueString()
	}
{{- end}}
{{- end}}
	return native, diags
}

// native{{.ResourceName}}ToTerraform converts native Go types to Terraform framework types.
// This is used after pathbind.Flatten to prepare output for state persistence.
func native{{.ResourceName}}ToTerraform(native *{{.ResourceName}}StateNative) *{{.ResourceName}}State {
	if native == nil {
		return nil
	}
	tf := &{{.ResourceName}}State{}
{{- range .AllFields}}
{{- if hasSuffix .Type "[]"}}
	tf.{{.GoName}} = toTerraformList(native.{{.GoName}})
{{- else if eq .Type "string"}}
	tf.{{.GoName}} = toTerraformString(native.{{.GoName}})
{{- else if eq .Type "*string"}}
	tf.{{.GoName}} = toTerraformString(stringPtrValue(native.{{.GoName}}))
{{- else if eq .Type "bool"}}
	tf.{{.GoName}} = toTerraformBool(native.{{.GoName}})
{{- else if eq .Type "*bool"}}
	tf.{{.GoName}} = toTerraformBoolPtr(native.{{.GoName}})
{{- else if eq .Type "int32"}}
	tf.{{.GoName}} = toTerraformInt64(int64(native.{{.GoName}}))
{{- else if eq .Type "*int32"}}
	{{- if and (not .Computed) (not .Required)}}
	tf.{{.GoName}} = toTerraformInt64Ptr(normalizeOptionalInt32ToInt64(native.{{.GoName}}))
	{{- else}}
	tf.{{.GoName}} = toTerraformInt64Ptr(int64Ptr(int32Ptr(native.{{.GoName}})))
	{{- end}}
{{- else if eq .Type "int64"}}
	tf.{{.GoName}} = toTerraformInt64(native.{{.GoName}})
{{- else if eq .Type "*int64"}}
	{{- if and (not .Computed) (not .Required)}}
	tf.{{.GoName}} = toTerraformInt64Ptr(normalizeOptionalInt64(native.{{.GoName}}))
	{{- else}}
	tf.{{.GoName}} = toTerraformInt64Ptr(native.{{.GoName}})
	{{- end}}
{{- else if eq .Type "map"}}
	tf.{{.GoName}} = toTerraformMap(native.{{.GoName}})
{{- else}}
	tf.{{.GoName}} = toTerraformString(native.{{.GoName}})
{{- end}}
{{- end}}
	return tf
}
